pipeline {
    agent any   // Runs on any available agent (slave)

    stages {
        stage('Build') {
            steps {
                echo 'Running build on any agent'
            }
        }
    }
}
